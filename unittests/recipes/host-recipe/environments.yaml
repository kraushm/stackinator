gcc-env:
  compiler:
      - toolchain: gcc
        spec: gcc@11.3.0
  unify: true
  specs:
  - osu-micro-benchmarks@5.9
  - libtree
  - cuda@11.8
  mpi:
    spec: cray-mpich
    gpu: cuda
  views:
    default:
    default:
      link: run

